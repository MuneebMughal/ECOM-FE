import React from "react";
import Layout from "../../layout/Layout";
const Delivery = (props) => {
  return (
    <Layout {...props}>
      <div>Delivery</div>
    </Layout>
  );
};

export default Delivery;
