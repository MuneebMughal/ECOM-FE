import React from "react";
import Layout from "../../layout/Layout";
const Reports = (props) => {
  return (
    <Layout {...props}>
      <div>Reports</div>
    </Layout>
  );
};

export default Reports;
