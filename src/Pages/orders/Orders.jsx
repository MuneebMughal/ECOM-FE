import React from "react";
import Layout from "../../layout/Layout";
const Orders = (props) => {
  return (
    <Layout {...props}>
      <div>Orders</div>
    </Layout>
  );
};

export default Orders;
